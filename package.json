{
  "name": "cisternas-site",
  "version": "1.0.0",
  "private": true,
  "description": "Personal website of Francisco Cisternas — teaching, research, analytics.",
  "scripts": {
    "preinstall": "node scripts/check-node.js",
    "dev": "dotenv -e .env.local -- tinacms dev -c \"next dev\"",
    "build": "next build",
    "dev:next-only": "next dev",
    "build:tina": "dotenv -e .env.local -- tinacms build --skip-cloud-checks && next build",
    "start": "next start",
    "lint": "next lint",
    "typecheck": "tsc --noEmit",
    "check:figures": "node scripts/check-figures.mjs"
  },
  "dependencies": {
    "@tinacms/cli": "^2.6.1",
    "next": "^16.3.1",
    "react": "19.1.1",
    "react-dom": "19.1.1",
    "three": "^0.185.1",
    "tinacms": "^3.12.1"
  },
  "devDependencies": {
    "@types/node": "22.10.2",
    "@types/react": "19.0.2",
    "@types/react-dom": "19.0.2",
    "@types/three": "^0.185.4",
    "dotenv-cli": "^11.0.0",
    "typescript": "5.7.2"
  },
  "engines": {
    "node": ">=20.9.0",
    "npm": ">=10"
  }
}
